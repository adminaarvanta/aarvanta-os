/** Rollback Home/Today without reverting the rest of A48. */
export const A48_TODAY_ENABLED = true;

/** Surgical studio prompt edits (copy/theme) instead of full regenerate. */
export const A48_STUDIO_EDITS = true;
