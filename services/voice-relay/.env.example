# Copy to /opt/aarvanta/voice-relay/.env on EC2

PORT=8090

# Same OpenAI key used by Aarvanta OS is fine
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Twilio auth token (for WebSocket signature validation)
TWILIO_AUTH_TOKEN=

# MUST exactly match the url= in TwiML ConversationRelay (and Vercel env)
# Path-based on onboarding host:
# VOICE_RELAY_WSS_URL=wss://onboarding.aarvanta.co/voice-relay/ws
# Or dedicated subdomain:
# VOICE_RELAY_WSS_URL=wss://voice.aarvanta.co/ws
VOICE_RELAY_WSS_URL=

# Optional: push transcripts into Voice OS after each call
# AARVANTA_VOICE_CALLBACK_URL=https://os.aarvanta.co/api/webhooks/voice-relay
# VOICE_RELAY_CALLBACK_SECRET=

# Optional: Knowledge Hub digest at call setup (defaults to …/api/voice/context
# derived from AARVANTA_VOICE_CALLBACK_URL)
# AARVANTA_VOICE_CONTEXT_URL=https://os.aarvanta.co/api/voice/context
# VOICE_RELAY_CONTEXT_TIMEOUT=1.5

# Custom Voice Agent clones (Instant Voice Clone). Same key as Vercel.
# ELEVENLABS_API_KEY=
# Optional public base for ConversationRelay play URLs (default: WSS host + /tts)
# VOICE_RELAY_TTS_PUBLIC_BASE=https://onboarding.aarvanta.co/voice-relay/tts
# VOICE_RELAY_TTS_DIR=/tmp/aarvanta-voice-tts
# VOICE_RELAY_TTS_TTL=120

# Reply style (defaults: short, low-bluff, less repetition)
# VOICE_RELAY_VERIFY_SIGNATURES=true
# VOICE_RELAY_MAX_TOKENS=90
# VOICE_RELAY_MAX_CHARS=280
# VOICE_RELAY_TEMPERATURE=0.45
# VOICE_RELAY_FREQUENCY_PENALTY=0.55
# VOICE_RELAY_PRESENCE_PENALTY=0.35
# Leave VOICE_AGENT_SYSTEM_PROMPT unset to use the built-in anti-bluff prompt.
# Spoken company/product name (default Aarvanta — do not use workspace brand names)
# VOICE_BRAND_NAME=Aarvanta
